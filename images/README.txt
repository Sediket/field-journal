Drop trip photos in this folder.

The "hero" image referenced in the trip JSON should be at the path
specified there (e.g., images/lake-blanche-2025/hero.jpg).

For the gallery, list any filenames in the trip JSON's "gallery" array.

Recommended:
- Resize photos to ~1600px wide before committing (keep repo size sane)
- Use .jpg for photos, .png for graphics
- Keep filenames lowercase, no spaces (use hyphens)
